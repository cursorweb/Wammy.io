/**
 * does magic
 * @param {*} socket Socket
 * @param {CanvasRenderingContext2D} ctx ctx
 */
export function draw(socket, ctx) {
  ctx.fillStyle = "red";
  ctx.fillRect(0, 0, innerWidth, innerHeight);
}